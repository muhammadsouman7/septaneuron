(() => {
  const canvas=document.querySelector('.hero #threadCanvas');
  if(canvas){const ctx=canvas.getContext('2d');let raf;let mouse={x:.5,y:.5},target={x:.5,y:.5};const resize=()=>{const d=Math.min(devicePixelRatio||1,2);canvas.width=canvas.clientWidth*d;canvas.height=canvas.clientHeight*d;ctx.setTransform(d,0,0,d,0,0)};const move=e=>{const r=canvas.getBoundingClientRect();target.x=(e.clientX-r.left)/r.width;target.y=(e.clientY-r.top)/r.height};canvas.addEventListener('pointermove',move);addEventListener('resize',resize);resize();const start=performance.now();const draw=now=>{const w=canvas.clientWidth,h=canvas.clientHeight;mouse.x+=(target.x-mouse.x)*.035;mouse.y+=(target.y-mouse.y)*.035;ctx.clearRect(0,0,w,h);const g=ctx.createRadialGradient(w*(.25+mouse.x*.12),h*.5,0,w*.5,h*.5,w*.7);g.addColorStop(0,'rgba(0,188,212,.16)');g.addColorStop(1,'rgba(0,0,20,0)');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);for(let i=0;i<18;i++){ctx.beginPath();for(let x=-20;x<w+20;x+=10){const nx=x/w,y=h*(.28+i*.026)+Math.sin(nx*8+(now-start)/2400+i*.65)*h*(.045+i*.003)+Math.sin(nx*19+(now-start)/3700)*10;x===-20?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.strokeStyle=`rgba(${i%3===0?'0,188,212':'27,65,145'},${.18-i*.006})`;ctx.lineWidth=i%4===0?1.3:.65;ctx.stroke()}raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw);addEventListener('pagehide',()=>cancelAnimationFrame(raf))}
  const reveal=[...document.querySelectorAll('[class*="-reveal"],.svc-section__head,.svc-detail,.svc-visual,.svc-flow,.svc-bottom-row,.svc-architecture,.svc-backend__list,.svc-principles,.svc-process__steps,.svc-tech__groups,.svc-engagement__grid,.svc-faq__list,.svc-media__layout')];const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce||!('IntersectionObserver' in window))reveal.forEach(x=>x.classList.add('is-visible'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -45px'});reveal.forEach(x=>io.observe(x))}
  const toggle=document.querySelector('.sn-nav__toggle'),nav=document.querySelector('.sn-nav__links');if(toggle&&nav)toggle.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',open)});
  document.querySelectorAll('.pf-filter button').forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter;document.querySelectorAll('.pf-filter button').forEach(x=>x.classList.remove('is-active'));button.classList.add('is-active');document.querySelectorAll('.pf-mini-card').forEach(card=>{card.hidden=filter!=='all'&&!((card.dataset.category||'').split(' ').includes(filter))})}));
})();
